---
name: 记忆库管理
description: 记忆库管理技能——整理/同步/检索第二大脑记忆库。当用户说"整理记忆""同步记忆""记忆入库""记忆搜索""记忆回顾""看看记忆库""按照记忆库的要求"时使用。自动扫描各AI工具（ZCode/ClaudeCode/Doubao等）的会话记录，清洗过滤后按结构导入记忆库，更新项目经验聚合，沉淀跨AI经验。
---

# 记忆库管理

将各AI工具的会话记忆清洗导入第二大脑记忆库（AI会话记忆\明细层 + 项目经验库\跨AI聚合 + 能力经验库\方法论），沉淀跨AI经验。所有路径从 `config.json` 读取，零硬编码，可分享给他人使用。

## 核心链路（本技能运作原理）

- **写入侧（沉淀）**：会话记录 → 提取 → 提炼（问题链卡/能力卡）→ 更新能力库（按能力独立成卡）
- **读取侧（应用）**：新任务 → 检索能力库（先检索再执行）→ 按经验执行 → 成功/失败回流更新
- 两半循环 = AI 持续进化：写入侧把"发生过的事"变成能力，读取侧用能力解决新问题，结果回流更新能力库

## When to Use（使用时机）

- 用户说"整理记忆 / 同步记忆 / 记忆入库 / 记忆搜索 / 记忆回顾 / **批量整理记忆**"（"批量整理记忆"=强制并行模式）
- 会话积累了较多内容需要沉淀时
- 新AI工具加入、需要接入记忆库时

## When NOT to Use（不要使用）

- 用户只做单次问答、无需沉淀时
- 用户明确说"不要记录"时
- 记忆库尚无 config.json 且用户未安装配置时（先运行 install.py）

## 执行流程（七步，按顺序）

### ⓪ 捕捉箱检查（先处理临时想法）

读取 `第二大脑\📥 捕捉箱.md`——若有未提炼条目，按统一标准提炼入库（判断归属项目/能力），处理后标记或清空。

### ① 扫描（自动发现 + 增量检测）

运行 `scripts/scan_ai_memories.py`：

- **纯自动发现（零手动配置）**：扫描本机用户目录+AppData，对照 `knowledge/ai_sources.md` 识别**所有安装过的AI**（当前覆盖24个：ZCode/ClaudeCode/豆包/Codex/Cursor/Windsurf/Trae/Kimi/通义/ChatGPT/WPSAI/MiniMax等，新AI在表中追加一行即可）
- **增量检测**：对照 `scripts/memory_hub_state.json` 只处理新增会话

**范围参数（可选）**：用户可指定只处理某个AI（如"只整理ZCode的"）或某个项目（如"只整理CPA考试"）——首次全量建议按AI分批，避免一次处理过多。

### ② 确认清单（先确认再提炼）

将新增清单按价值分类展示给用户：

- **✅ 收录**：复习/工作/知识产出类
- **🗑️ 过滤**：闲聊/购物/空会话/隐私敏感/纯系统消息（对照 config.json 的 ignore_keywords）

请用户确认（或默认采纳AI分类），确认后进入提炼。

### ②½ 批量并行模式（两种触发）

**触发规则**：
- **自动触发**：未处理数量 > 10 时自动启用
- **显式触发**：用户说"批量整理记忆"时强制启用（即使数量少）

**执行步骤**：

1. 运行 `scripts/split_batch.py --workers 3` 把清单切成N批（默认3批，可调）
2. **为每批启动一个子代理并行执行**（子代理提示词：处理指定文件 → 按 `knowledge/模板/会话笔记模板.md` 统一标准提炼 → 写入 `AI会话记忆\<对应AI>\` → **不写执行史、不更新状态**，只返回提炼结果清单）
3. 所有子代理完成后，主Agent汇总：统一执行 `scripts/append_execution.py` 聚合执行史（**串行写入防冲突**）→ 统一 `--mark` 更新状态
4. 每批子代理内仍遵守"每轮≤5个"（多批并行时总量 = 批数×5）

**冲突防护**：子代理只写各自独立的笔记文件（文件名含会话ID不冲突）；执行史/状态文件由主Agent统一串行更新。

### ③ 提炼（分批执行，控制token）

每次会话最多提炼 **5 个**，分多轮推进（状态文件记录进度）。提炼规则：

- 读会话内容 → 判断价值（按②标准）
  - ZCode 会话：查询 `config.json` 中 sqlite 路径的 `searchable_text` 字段获取内容摘要
  - ClaudeCode 会话：直接读 projects 目录下 jsonl 文件
  - 豆包：本地无对话正文（对话在云端）——默认仅索引产出文件；**若用户在豆包导出对话并放入 config.json 的 import_dir 目录**，则按统一标准提炼对话（四板块）
- 每轮最多5个，**提炼完成后运行 `scripts/scan_ai_memories.py --mark <文件路径...>` 标记已处理**（增量定位核心：下次扫描自动跳过，不会每次全过）
- 按"结论（成果）/待办/知识点/问题与解决/经验/决策"提炼（模板见 `knowledge/模板/会话笔记模板.md`）——**聚焦问题→解决→成果→经验**，与CPA考点记忆（易忘清单）是两套体系，不混入
- 标记规则：🔴问题→✅解决｜⭐可复用经验（一句话）｜💡薄弱点（用户反复问的）｜🧭偏好（写入frontmatter preferences字段）
- **无关紧要内容不导入**；🔒敏感内容标注且禁止外发
- **内容级隐私检测**：提炼时AI自动判断——涉密/未定稿/客户未确认数据/个人隐私信息 → **不导入记忆库**（或仅标注🔒存本地）；拿不准的问用户

### ④ 导入（写入明细层）

提炼结果写入 `AI会话记忆\<AI名>\<项目名>\` 目录，文件命名 `<日期> <主题>.md`。

### ⑤ 聚合（更新项目经验库）——按"问题链经验卡"格式

- 向对应项目的 `📊 项目经验聚合.md` 写入/更新**问题链经验卡**（升级版格式，见 `knowledge/模板/会话笔记模板.md`）：
  `触发场景 → 目标 → 执行过程多轮 → 结果+验证次数 → 根因(why) → 复用(if-then 分保持/改进/停止/尝试) → 边界 → 链接`
- **提炼判断标准**：用户反复追问/纠正≥2轮或执行失败过 → 完整问题链卡；一次通过无波折 → 只写一句话结论（⭐）
- **每轮用户纠正=一个难点**，必须记录；**必须回答 why**（只记 what/how 不记 why = 存档不是经验）
- **维护三原则**：①筛选——只保留推动解决的有效轮次，"不了了之"轮次删除 ②归纳——提炼成"解决了什么"，非原话堆砌 ③持续更新——同问题新经验替换旧解法/追加新难点/删过时内容，保持"当前最优版本"
- 可跨项目复用的经验 → `能力经验库\对应能力\`，**按能力独立成文件**（两种形态）：
  - **问题链卡**（时间轴叙事）：有反复追问/纠正的复杂任务 → 保留多轮过程
  - **能力卡**（方法论浓缩）：高频可复用能力 → 浓缩为"触发场景/目标/方法论/复用(if-then)/链接"，不留过程
- **能力库结构原则（用户确认）**：目录=能力域（粗）｜文件=单一能力（细）｜`📋 经验总索引.md` 问题→文件直连——检索命中即达能力文件，不在大文件里翻找
- **拆分粒度**：域聚合页有"能力清单"表（能力|文件|状态）；新经验先入域聚合，**满3条拆成独立能力文件**；新增能力文件后同步更新域聚合清单+总索引
- 同步更新该项目的**摘要区**（当前状态+最近问题链）
- `append_execution.py` 仍可用于快速追加执行史行（简版记录）

### ⑤½ 提炼质检（质量保障）

每次整理完成后，主Agent**抽查1-2条**新经验卡：是否符合升级版问题链格式（触发场景/目标/多轮/结果+验证/根因why/if-then复用/边界/链接）？**难点是否来自用户真实追问？是否回答了 why？** 不符合则修正——保证不同AI提炼质量一致。

### ⑥ 更新状态

- 更新 `scripts/memory_hub_state.json`（记录已处理位置）
- 更新 `项目经验库\📋 项目总索引.md`（项目状态/最近更新）
- 更新 `能力经验库\📋 经验总索引.md`（新增/更新能力文件后同步：关键词+文件路径+状态）
- **索引一致性检查（强制）**：索引只标注真实存在的经验——"✅"必须对应实际文件；尚未提炼的标"⏳待提炼"或"待建"，禁止标✅（防止AI检索到不存在的经验）

### ⑦ 归档（项目完成时）

项目结束后（如考试结束/客户结项）：将 `项目经验库\<项目名>\` 移入 `项目经验库\归档\<项目名>-<年份>\`，并在 `📋 项目总索引.md` 标注"已归档"。

## 核心执行模式（所有任务，不只整理记忆时）

**AI 收到用户任何问题/任务时**：
1. **先检索**：运行 `scripts/search_memory.py "问题关键词"`——查记忆库有没有同类问题链
2. **引用经验**：有同类经验 → 基于问题链经验卡组织回复（引用成功路径+避开已知难点）；在回复中标注"（依据记忆库经验）"
3. **执行**：按经验卡操作；无同类经验 → 按专业能力执行，注意记录过程
4. **沉淀**：任务完成后——成功/失败都要更新：有经验卡→追加新轮次；新问题→创建新问题链经验卡（问题起点→执行过程多轮→结果→难点→复用要点）

**闭环**：问题 → 检索 → 引用经验 → 执行 → 沉淀新经验 → 下次更准

**应用闭环（引用计数+失败回流，提高成功率的核心机制）**：
- AI 引用某条经验卡完成任务后，运行 `scripts/search_memory.py --record-use "<库名>/<问题链N>"` 记录引用（累计次数+最近日期）
- **复用仍失败 → 当场回流**：把失败原因追加进该经验卡（补新难点/换新解法），保持"当前最优版本"
- 搜索时会展示命中经验卡的引用状态——多次复用成功的升为"金牌经验"，长期未复用的标记"待验证"（考虑清理或重新提炼）
- 引用计数存 `memory_hub_state.json` 的 `experience_usage`（本地脚本，零token成本）

## 记忆搜索（/记忆搜索 命令）

用户说"记忆搜索"或"搜索记忆库"时：运行 `scripts/search_memory.py "关键词"`，跨三层（项目库/能力库/AI记忆/捕捉箱）定位命中记录，然后按分层读取规则深入对应文件。搜索结果不足时建议写入经验库。
**自动扩词**：搜索前AI先扩展关键词的同义词/近义词（如"收入确认"→"验收/控制权转移/未结算"），多词分别搜再合并——弥补关键词匹配的局限

## 记忆回顾（/记忆回顾 命令）

用户说"记忆回顾"时执行：汇总各项目执行史 → 提炼新的可复用方法论进 `能力经验库\` → 更新《CPA复习经验总结》类项目经验文件 → 检查捕捉箱是否有堆积。

## 分类判定标准（经验放哪）

> **"这个经验，换一个项目还能不能用？"**
> - 能 → 能力经验库（Excel自动化方法/审计底稿套路/学习方法）
> - 不能 → 项目经验库（CPA战略错题/某客户具体事项）

**能力粒度规则**：一个能力一个文件——如"收入确认与列报""附注格式规范""红按钮宏开发"各一卡；同类多条（≥3）才拆，避免碎片化空目录

## 检索规则（省token，分层读）

1. 先读 `能力经验库\📋 经验总索引.md`（10秒定位：问题关键词→能力文件直连）
2. 再读对应项目聚合文件**摘要区**（3-5行状态）
3. 需要细节才读执行史全文/跳转 `AI会话记忆\<AI名>\`

## 熔断与预算

- 同一操作失败 **2 次** → 停止并报告原因，不自行换方案重试
- 单轮提炼上限 **5 个会话**；扫描无新增时直接报告"无新增"
- 提炼中发现无法判断的内容 → 列出来让用户决定，不臆断

## 隐私保护（强制）

- 记忆库含审计项目、个人信息——**禁止将库内容外发**（不粘贴到联网服务、不上传）
- 🔒 标注的内容：不得复制转发，仅本地使用

## 配置说明

- 首次使用：运行 `install.py` 生成 `config.json`（记忆库根目录+各AI源路径，全可自定义）
- 参考 `config.json.example`；路径含中文/空格用绝对路径
- 分享给他人：打包整个技能目录，对方运行 install.py 填自己的路径

## 参考

- 完整结构与读写规范：`D:\第二大脑\📖 记忆库使用指南.md`
- 模板：`knowledge/模板/会话笔记模板.md`
