---
name: 记忆库管理
description: 记忆库管理技能——整理/同步/检索第二大脑记忆库。当用户说"整理记忆""同步记忆""记忆入库""记忆搜索""记忆回顾""看看记忆库""按照记忆库的要求"时使用。自动扫描各AI工具（ZCode/ClaudeCode/Doubao等）的会话记录，清洗过滤后按结构导入记忆库，更新项目经验聚合，沉淀跨AI经验。
---

# 记忆库管理

将各AI工具的会话记忆清洗导入第二大脑记忆库（AI会话记忆\明细层 + 项目经验库\跨AI聚合 + 能力经验库\方法论），沉淀跨AI经验。所有路径从 `config.json` 读取，零硬编码，可分享给他人使用。

## When to Use（使用时机）

- 用户说"整理记忆 / 同步记忆 / 记忆入库 / 记忆搜索 / 记忆回顾"
- 会话积累了较多内容需要沉淀时
- 新AI工具加入、需要接入记忆库时

## When NOT to Use（不要使用）

- 用户只做单次问答、无需沉淀时
- 用户明确说"不要记录"时
- 记忆库尚无 config.json 且用户未安装配置时（先运行 install.py）

## 执行流程（七步，按顺序）

### ⓪ 捕捉箱检查（先处理临时想法）

读取 `第二大脑\📥 捕捉箱.md`——若有未提炼条目，按统一标准提炼入库（判断归属项目/能力），处理后标记或清空。

### ① 扫描（自动发现 + 增量检测）

运行 `scripts/scan_ai_memories.py`：

- **纯自动发现（零手动配置）**：扫描本机用户目录+AppData，对照 `knowledge/ai_sources.md` 识别**所有安装过的AI**（当前覆盖24个：ZCode/ClaudeCode/豆包/Codex/Cursor/Windsurf/Trae/Kimi/通义/ChatGPT/WPSAI/MiniMax等，新AI在表中追加一行即可）
- **增量检测**：对照 `scripts/memory_hub_state.json` 只处理新增会话

**范围参数（可选）**：用户可指定只处理某个AI（如"只整理ZCode的"）或某个项目（如"只整理CPA考试"）——首次全量建议按AI分批，避免一次处理过多。

### ② 确认清单（先确认再提炼）

将新增清单按价值分类展示给用户：

- **✅ 收录**：复习/工作/知识产出类
- **🗑️ 过滤**：闲聊/购物/空会话/隐私敏感/纯系统消息（对照 config.json 的 ignore_keywords）

请用户确认（或默认采纳AI分类），确认后进入提炼。

### ③ 提炼（分批执行，控制token）

每次会话最多提炼 **5 个**，分多轮推进（状态文件记录进度）。提炼规则：

- 读会话内容 → 判断价值（按②标准）
  - ZCode 会话：查询 `config.json` 中 sqlite 路径的 `searchable_text` 字段获取内容摘要
  - ClaudeCode 会话：直接读 projects 目录下 jsonl 文件
  - 豆包：本地无对话正文（对话在云端）——默认仅索引产出文件；**若用户在豆包导出对话并放入 config.json 的 import_dir 目录**，则按统一标准提炼对话（四板块）
- 按"结论（成果）/待办/知识点/问题与解决/经验/决策"提炼（模板见 `knowledge/模板/会话笔记模板.md`）——**聚焦问题→解决→成果→经验**，与CPA考点记忆（易忘清单）是两套体系，不混入
- 标记规则：🔴问题→✅解决｜⭐可复用经验（一句话）｜💡薄弱点（用户反复问的）｜🧭偏好（写入frontmatter preferences字段）
- **无关紧要内容不导入**；🔒敏感内容标注且禁止外发

### ④ 导入（写入明细层）

提炼结果写入 `AI会话记忆\<AI名>\<项目名>\` 目录，文件命名 `<日期> <主题>.md`。

### ⑤ 聚合（更新项目经验库）

- 运行 `scripts/append_execution.py` 向对应项目的 `📊 项目经验聚合.md` 追加执行史一行
- **"沉淀经验"列必填**（一句话可复用结论，禁止空着或只写"完成"）
- 同步更新该项目的**摘要区**（当前状态+最近执行）
- 可复用方法论进 `能力经验库\对应能力\`

### ⑥ 更新状态

- 更新 `scripts/memory_hub_state.json`（记录已处理位置）
- 更新 `项目经验库\📋 项目总索引.md`（项目状态/最近更新）

### ⑦ 归档（项目完成时）

项目结束后（如考试结束/客户结项）：将 `项目经验库\<项目名>\` 移入 `项目经验库\归档\<项目名>-<年份>\`，并在 `📋 项目总索引.md` 标注"已归档"。

## 记忆回顾（/记忆回顾 命令）

用户说"记忆回顾"时执行：汇总各项目执行史 → 提炼新的可复用方法论进 `能力经验库\` → 更新《CPA复习经验总结》类项目经验文件 → 检查捕捉箱是否有堆积。

## 分类判定标准（经验放哪）

> **"这个经验，换一个项目还能不能用？"**
> - 能 → 能力经验库（Excel自动化方法/审计底稿套路/学习方法）
> - 不能 → 项目经验库（CPA战略错题/某客户具体事项）

## 检索规则（省token，分层读）

1. 先读 `项目经验库\📋 项目总索引.md`（10秒定位项目）
2. 再读对应项目的聚合文件**摘要区**（3-5行状态）
3. 需要细节才读执行史全文/跳转 `AI会话记忆\<AI名>\`

## 熔断与预算

- 同一操作失败 **2 次** → 停止并报告原因，不自行换方案重试
- 单轮提炼上限 **5 个会话**；扫描无新增时直接报告"无新增"
- 提炼中发现无法判断的内容 → 列出来让用户决定，不臆断

## 隐私保护（强制）

- 记忆库含审计项目、个人信息——**禁止将库内容外发**（不粘贴到联网服务、不上传）
- 🔒 标注的内容：不得复制转发，仅本地使用

## 配置说明

- 首次使用：运行 `install.py` 生成 `config.json`（记忆库根目录+各AI源路径，全可自定义）
- 参考 `config.json.example`；路径含中文/空格用绝对路径
- 分享给他人：打包整个技能目录，对方运行 install.py 填自己的路径

## 参考

- 完整结构与读写规范：`D:\第二大脑\📖 记忆库使用指南.md`
- 模板：`knowledge/模板/会话笔记模板.md`
